# Appointment-management-system
 A command-line appointment system for adding, validating, and storing patient appointments. Key features: auto-generate or enter patient ID, record details (name, age, reason, priority, time), validate inputs, display appointments in a table, and search by patient ID.
